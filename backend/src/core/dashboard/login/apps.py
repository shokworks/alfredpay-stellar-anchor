from django.apps import AppConfig


class LoginConfig(AppConfig):
    name = "core.dashboard.login"
    verbose_name = "Alfred Pay Dashboard Sandbox Login"
    default_auto_field = "django.db.models.BigAutoField"
